To achieve your goal of visualizing the gating logits on distribution, you will need to capture the outputs of your routing mechanism just before the selection step. In a MoE or MoLE architecture, the router transforms the hidden states into raw logits, which are then passed through a gating function (like Top-K selection or softmax) to determine the selection probabilities.

By extracting these raw logits during the forward pass, you can visualize the distribution to monitor expert utilization and ensure your system isn't suffering from load imbalance (e.g., where the Python adapter is incorrectly dominating the routing). Visualizing this distribution is also a critical diagnostic tool for implementing "router z-loss," a technique that penalizes excessively high router logits during training to prevent the gating network from becoming overly confident and numerically unstable.